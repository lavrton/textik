import { assert } from 'chai';

import { createCarota } from './test-utils';

describe('Text', function () {
  // ======================================================
  it('hello words', function () {
    const editor = createCarota();
    editor.load([]);
  });
});
